---
import type { CollectionEntry } from 'astro:content'
import Article from '../page/Article.astro'
type Props = {
  person: CollectionEntry<'authors'>
}
const { person }: Props = Astro.props
const { name, bio } = person.data
---

<Article title={name.display}>
    <Fragment set:html={bio} />
</Article>
