---
import { formatDate } from '@config/date'
export type Props = {
  date: Date
}
const { date }: Props = Astro.props
const formattedDate = formatDate(date, Astro.currentLocale)
---

<time datetime={date.toISOString()}>{formattedDate}</time>
