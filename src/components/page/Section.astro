---
type Props = {
  title?: string
}
const { title }: Props = Astro.props
---

<section class="box">
  {title && (
          <header>
              <h2 class="title is-4">{title}</h2>
          </header>
  )}
  <slot />
</section>
